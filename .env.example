# Copy to .env and fill in from the Supabase dashboard (Project Settings → API / Database).
# .env itself is gitignored — never commit real values.

SUPABASE_DB_PASSWORD=
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=
